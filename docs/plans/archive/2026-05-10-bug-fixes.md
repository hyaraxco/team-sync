# Bug Fixes Plan

> **Status:** PENDING

**Goal:** Fix bugs and code smells found in audit — payslip PDF, attendance race condition, frontend error handling, Axios config, format utilities.

**Tech Stack:** Laravel 12, PHP 8.2, Vue 3, Pinia, Axios, Luxon

---

### Task 1: Implement Allowances & Bonus in PayslipPdfService

**File:** `team-sync-be/app/Services/PayslipPdfService.php:27-28`

**Current:** `$allowances = 0; $bonus = 0;` (stubbed)

**Approach:** Check if PayrollDetail has allowance/bonus fields. If yes, use them. If not, check if they should be calculated from other sources.

- [ ] Read PayslipPdfService and PayrollDetail model to understand data flow
- [ ] Implement allowance/bonus retrieval
- [ ] Add test for payslip with allowances/bonus

---

### Task 2: Add `lockForUpdate()` to Attendance Clock-in

**File:** `team-sync-be/app/Repositories/AttendanceRepository.php:204-337`

**Current:** Uses `DB::transaction()` but no row locking. Concurrent clock-in requests could duplicate entries.

**Approach:** Add `lockForUpdate()` on the attendance record query within the transaction.

- [ ] Wrap attendance lookup with `lockForUpdate()`
- [ ] Add concurrent clock-in test

---

### Task 3: Replace `console.error` with Toast Notifications (Frontend)

**Files (36 instances):**
- `TaskDetailModal.vue` (14 occurrences)
- `option.js` store (10 occurrences)
- `EmployeeStatistics.vue`, `TeamPulseOverview.vue`, `ReviewDetail.vue`
- Various other components

**Approach:** Replace `.catch(() => { console.error(...) })` with toast notification using `useToast()` composable.

- [ ] Fix `TaskDetailModal.vue` — 14 catch blocks
- [ ] Fix `option.js` store — 10 catch blocks
- [ ] Fix remaining components

---

### Task 4: Fix `formatCurrency` to Use IDR Locale

**File:** `team-sync-fe/src/utils/formatUtils.js`

**Current:** `formatCurrency` uses `en-US` / `USD`. Should use `id-ID` / `IDR`.

**Approach:** Update or deprecate `formatCurrency`. Ensure all callers use `formatRupiah` or `formatIDR` instead.

- [ ] Check all usages of `formatCurrency`
- [ ] Update to IDR locale or replace with `formatRupiah`
- [ ] Verify no USD formatting is needed

---

### Task 5: Create Isolated Axios Instance

**File:** `team-sync-fe/src/plugins/axios.js`

**Current:** Modifies `axios.defaults` globally. Should use `axios.create()`.

**Approach:** Create isolated instance with `axios.create()`, export it. Update all store imports.

- [ ] Refactor `plugins/axios.js` to use `axios.create()`
- [ ] Verify all stores import correctly
- [ ] Test 401 handling still works

---

### Task 6: Resolve Duplicate 401 Handling

**Files:**
- `team-sync-fe/src/plugins/axios.js` — response interceptor
- `team-sync-fe/src/stores/auth.js` — checkAuth
- `team-sync-fe/src/router/index.js` — registerAuthGuard

**Current:** 401 can trigger hard redirect AND Vue Router navigation simultaneously.

**Approach:** Single source of truth for 401 handling. Let Axios interceptor handle 401 → clear token → redirect. Remove duplicate handling from auth store and router guard.

- [ ] Design single 401 flow
- [ ] Remove duplicate handlers
- [ ] Test logout flow end-to-end
