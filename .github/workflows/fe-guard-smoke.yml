name: FE Guard Smoke

on:
  workflow_dispatch:
  pull_request:
    paths:
      - ".github/workflows/fe-guard-smoke.yml"
      - "team-sync-fe/**"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/fe-guard-smoke.yml"
      - "team-sync-fe/**"

concurrency:
  group: fe-guard-smoke-${{ github.ref }}
  cancel-in-progress: true

jobs:
  fe-guard-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Restore Bun install cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.bun/install/cache
            team-sync-fe/node_modules
          key: ${{ runner.os }}-bun-guards-${{ hashFiles('team-sync-fe/bun.lock', 'team-sync-fe/package.json') }}
          restore-keys: |
            ${{ runner.os }}-bun-guards-

      - name: Install FE dependencies
        working-directory: team-sync-fe
        run: bun install --frozen-lockfile

      - name: Run FE guard smoke tests
        working-directory: team-sync-fe
        run: bun run test:guards
